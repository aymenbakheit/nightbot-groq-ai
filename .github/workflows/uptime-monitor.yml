name: Uptime Monitor

on:
  schedule:
    - cron: '*/5 * * * *'  # كل 5 دقائق
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Nightbot Groq AI
        run: |
          echo "🔔 Pinging Nightbot Groq AI..."
          curl -s -o /dev/null -w "%{http_code}" https://aymenbakheit.github.io/nightbot-groq-ai
          echo ""
          echo "✅ Ping successful!"
